# 사주 팟캐스트 에피소드 추출 프로그램 — VS Code 업무지시서

이 문서를 VS Code의 Codex에 통째로 전달한다. **오늘 내 컴퓨터에서 실행 가능한 1일 MVP**를 만든다. 기존 회의 녹취록은 검증 자료이며, 프로그램은 내 컴퓨터에서 실행한다.

## 1. 목표

긴 음성 녹음을 넣으면 시간 표시 전사를 만들고, AI가 에피소드 후보와 시작·끝 시간을 제안한다. 사용자는 음성을 미리 듣고 제목과 시간대를 고친 뒤 후보별로 보류 또는 승인한다. **승인된 구간만** 오디오 파일로 잘라 전사문과 함께 저장하고, 승인된 전사문만 임베딩하여 검색 인덱스에 넣는다. 원본은 수정하지 않으며, 게시·배포는 자동화하지 않는다.

임베딩은 음성을 자르는 기술이 아니다. 제목·질문·전사문을 검색용 벡터로 만드는 단계이므로 승인 이후에 실행한다.

## 2. 환경과 기술

- Windows, VS Code, Python 가상환경(미니콘다 가능).
- 화면: Streamlit. 음성 변환·분할: FFmpeg/ffprobe. 저장: 로컬 폴더와 SQLite.
- 전사·후보 분석·임베딩은 독립 함수로 나눈다. OpenAI API 모델명은 설정에서 바꿀 수 있게 한다.
- API 키는 환경변수 또는 로컬 `.env`에서만 읽는다. 키·원본·전사·결과·DB를 Git에 올리지 않도록 `.gitignore`를 작성한다.
- 외부 API로 음성이 전송되며 비용이 생길 수 있음을 분석 시작 전에 표시하고, 사용자가 버튼을 누른 뒤에만 호출한다.
- 동일 파일을 다시 열면 중간 결과를 복원한다. 화면 새로고침만으로 API를 재호출하지 않는다.

## 3. 화면과 기능

### A. 업로드·전사

1. mp3/m4a/wav/mp4 등의 음성 파일을 넣는다.
2. 같은 녹음의 시간 표시 `.txt`를 선택적으로 함께 넣으면 전사를 건너뛴다. `.txt`만 있으면 후보 분석은 가능하지만 미리듣기·음성 분할은 비활성화한다.
3. 파일명, 길이, 진행 상태를 보여준다. 긴 음성은 업로드 제한 이하의 압축된 조각으로 나눠 전사한 뒤 각 조각의 상대 시간을 **원본 전체 시간**으로 합친다.
4. 타임코드가 불명확하거나 역순이면 오류를 알린다. 전사와 분석 결과는 다음 실행에 재사용한다.

### B. 후보 검토

각 후보 카드에 제목, 청취자 질문, 추천 이유, 원본 시작·끝 시간, 해당 전사문, 활용 유형(`원본 클립`/`새 녹음 소재`/`짧은 클립`), 공개 전 확인할 표현을 표시한다. 원본 구간 미리듣기, 시작·끝 시간 수정, 제목 수정, `보류`·`승인`을 제공한다.

후보의 기본 단위는 **한 개의 연속 구간**이다. 떨어진 두 구간이 같은 주제이면 관련 후보로 묶어 표시하되 자동으로 이어 붙이지 않는다. 제안 시간에 실제 발화가 없거나 길이를 벗어나면 후보를 폐기한다.

### C. 승인 후 처리

`승인한 후보 처리` 버튼을 누른 뒤 승인된 구간에 대해서만 다음을 한다.

1. 원본에서 해당 시간만 `.mp3`로 잘라 별도 저장한다. `.txt`만 입력한 경우 음성 파일은 만들지 않는다.
2. 동일 이름의 구간 전사 `.txt`와 제목·시간·원본명·승인 일시가 담긴 `.json`을 만든다.
3. 제목·질문·구간 전사문을 임베딩하고 SQLite에 벡터, 음성 경로, 원본 시간, 승인 상태를 기록한다.
4. 결과 폴더 또는 ZIP 내려받기를 제공하고 후보별 성공·실패를 표시한다.
5. 동일 후보와 동일 시간으로 재처리할 때 파일과 DB 행이 중복되지 않게 한다. 시간 수정 후 재승인했다면 버전을 구별한다.

## 4. AI 후보 선정 기준

- **청취자의 질문이 있는가?** 기존 멤버를 모르면 이해하기 어려운 내부 잡담은 낮게 평가한다.
- **대화가 전개되는가?** 질문, 경험, 다른 해석, 반응이 있는 부분을 우선한다.
- **출연자들의 호흡이 드러나는가?** 단순히 웃긴 장면과 한 편의 이야기 소재를 구분한다.
- **현재 녹음이 완결되는가?** 짧지만 좋은 질문은 `새 녹음 소재`로 표시한다.
- **공개 검토가 필요한가?** 제삼자 실명·사생활·내부 갈등이 포함되면 별도 표시한다. AI 판단은 참고이며 공개 여부는 사람이 결정한다.

실제 발화나 시간을 지어내지 않는다. 첫 검증 자료의 기대 후보 예시는 `사주는 왜 이렇게 어렵게 말할까?`, `결국 사주도 사람 이야기다`, `모임 대신 팟캐스트를 하게 된 과정`이다. 제목을 하드코딩하지 말고 녹취에서 근거를 찾는다.

## 5. 저장 구조

```text
podcast_episode_tool/
  app.py
  pipeline/                    # 전사·분석·분할·임베딩
  data/
    recordings/<녹음ID>/
      original.<확장자>         # 원본 보존
      transcript.json          # 발화별 시작·끝과 텍스트
      candidates.json          # 후보와 보류·승인 상태
      clips/<후보ID>_제목.mp3
      clips/<후보ID>_제목.txt
      clips/<후보ID>_제목.json
    index.sqlite3              # 승인된 텍스트의 검색 인덱스
  requirements.txt
  .env.example
  .gitignore
  README.md
```

후보와 전사는 중간 결과로 보존하지만 **승인 전 후보는 검색 인덱스에 넣지 않는다.** 벡터는 전사 텍스트에 대한 것이며 음성 클립 경로와 연결한다.

## 6. 오늘의 범위

### 반드시 구현

- 음성 1개 또는 시간 표시 녹취록 입력, 원본 길이 기준 타임코드 생성.
- 적절한 후보 0~5개 제안. 후보별 시간·제목 수정, 미리듣기, 보류·승인.
- 승인된 것만 음성 분할·전사 저장·임베딩·SQLite 기록.
- 오류 표시, 원본 보존, 재실행 복원, 중복 처리 방지.
- Windows/VS Code/미니콘다 설치와 실행 명령을 적은 README.

### 후속으로 남김

자동 게시, 얼굴·실명 식별, BGM·쇼츠 제작, 고급 편집, 서로 떨어진 구간의 자동 연결, AI의 최종 공개 승인.

## 7. 검증과 완료 보고

1. 첨부된 `사주모임 운영진 회의.m4a.txt`로 실제 발화와 후보 타임코드를 대조한다.
2. 짧은 테스트 음성으로 미리듣기와 분할 음성의 실제 길이를 확인한다.
3. 한 후보만 승인했을 때 보류 후보의 음성·임베딩·DB 행이 생기지 않는지 확인한다.
4. 앱을 껐다 켠 뒤 승인 상태가 복원되고 재처리해도 중복이 없는지 확인한다.
5. API 키가 없으면 키를 코드에 요구하거나 임의로 만들지 말고, `.txt` 입력과 모의 응답으로 승인 흐름을 검증한 뒤 실제 API 경로는 미검증이라고 보고한다.

완료 시 **실행 명령, 구현 범위, 검증 결과, 남은 제약**을 간결하게 보고한다.

## 8. 공식 문서

- [OpenAI 음성 전사](https://developers.openai.com/api/docs/guides/speech-to-text)
- [OpenAI 임베딩](https://developers.openai.com/api/docs/guides/embeddings)
- [OpenAI 구조화 출력](https://developers.openai.com/api/docs/guides/structured-outputs)
